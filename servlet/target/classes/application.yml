server:
  port: 8080
  servlet:
    context-path: ""

#注意时区
spring:
  datasource:
    name: personnelmanagement
    url: jdbc:mysql://localhost:3306/personnelmanagement
    username: root
    password: as145768
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.personnelmanagement.bean
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
